{
  "name": "product-page",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npx parcel ./index.pug",
    "build": "mkdir dist && cp -r ./assets ./dist && cp -r ./draco ./dist && npx parcel build ./index.pug"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.11.0",
    "parcel": "^2.7.0",
    "simple-color-picker": "^1.0.5",
    "three": "^0.143.0",
    "tweakpane": "^3.1.0"
  },
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.7.0",
    "@parcel/transformer-pug": "^2.7.0",
    "@tweakpane/core": "^1.1.0",
    "@types/three": "^0.143.2"
  }
}
