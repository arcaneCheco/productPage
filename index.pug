doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

    base(href="/")
    
    title="Product - Concept"

    link(rel="stylesheet" type="text/css" href="index.css")

  body
    #canvas
    #canvas2d
    #customizePanel
        include ./views/customizePanel.pug
    #markers
        each id in ["tire", "rim", "break"]
            include ./views/infoLabel.pug
    p.title
    p.toAssembly="Back to Assembly"
    .viewButtons
        .explode 
            p="Explode"
        .assemble
            p="Assemble"
    script(type="module" src="app.ts")